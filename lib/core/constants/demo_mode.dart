/// Toggle flag for demo / design-review mode.
///
/// Set [kDemoMode] = true  → bypasses all API calls, auto-logs in, uses mock data.
/// Set [kDemoMode] = false → normal production behaviour.
///
/// After changing this value, hot-restart the app (press R in terminal).
const bool kDemoMode = false;
